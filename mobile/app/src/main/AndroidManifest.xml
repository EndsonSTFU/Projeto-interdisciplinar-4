<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.ProjetoInterdisciplinar3"
            tools:targetApi="31">

        <!-- MainActivity (entry point) -->
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".activity.CadastroPacienteActivity" />
        <activity android:name=".activity.SelecaoAtendimentoActivity" />
        <activity android:name=".activity.CadastroColaboradorActivity" />
        <activity android:name=".activity.DashboardColaboradorActivity" />
        <activity android:name=".activity.DashboardPacienteActivity" />
        <activity android:name=".activity.AgendarHorarioActivity" />
        <activity android:name=".activity.AnotacoesPacientesActivity" />
        <activity android:name=".activity.CadastrarHorarioActivity" />
        <activity android:name=".activity.DetalhesAnotacaoActivity" />
        <activity android:name=".activity.DetalhesPacienteActivity" />
        <activity android:name=".activity.SalvarAnotacaoActivity" />
        <activity android:name=".activity.VerificarSenhaActivity" />

    </application>

</manifest>
